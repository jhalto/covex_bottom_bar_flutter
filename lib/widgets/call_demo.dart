import 'package:flutter/material.dart';

class CallDemo extends StatelessWidget {
  const CallDemo({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: AppBar(title: Text("Call"),),
    );
  }
}
