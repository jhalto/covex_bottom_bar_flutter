import 'package:flutter/material.dart';

class AppsDemo extends StatelessWidget {
  const AppsDemo({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(
      title: Text("Apps"),
    );
  }
}
