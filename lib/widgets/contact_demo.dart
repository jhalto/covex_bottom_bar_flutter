import 'package:flutter/material.dart';

class ContactDemo extends StatelessWidget {
  const ContactDemo({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(
      title: Text("Contact"),
    );
  }
}
